roles:
  protected_columns: ["id"]

parsing:
  column_types:
    age: "numeric"

standardization:
  global_trim_whitespace: true

missing_data:
  imputation:
    numeric:
      strategy: "median"
      allow_if_missing_pct_leq: 30.0
